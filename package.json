{
  "name": "lyric-library",
  "private": true,
  "version": "1.0.0",
  "description": "Lyric Library monorepo — mobile app, admin dashboard, shared backend",
  "scripts": {
    "mobile": "npm --prefix mobile-app start",
    "mobile:web": "npm --prefix mobile-app run web",
    "admin": "npm --prefix admin-dashboard run dev",
    "backend": "npm --prefix backend run dev",
    "backend:db": "npm --prefix backend run db:push"
  }
}
