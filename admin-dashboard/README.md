# Lyric Library Admin Dashboard

React + TypeScript admin studio for artist/song management and music API sync.

## Setup

Start the backend first on port 4000, then:

```bash
cd admin-dashboard
npm install
npm run dev
```

Open `http://localhost:5173`

Sign in with the backend admin credentials.
